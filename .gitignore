__pycache__/
*.pyc
.pytest_cache/
.venv/
# local price cache (rebuilt from the network / restored from CI cache)
cache/
*.sqlite
*.sqlite-*

# Scan output - regenerated by the nightly workflow and published straight to
# Pages from the runner's working tree, so it never needs to live in git.
docs/data/
